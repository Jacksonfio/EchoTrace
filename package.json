{
  "name": "echotrace-ai",
  "private": true,
  "workspaces": [
    "apps/*",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:server\"",
    "dev:frontend": "npm run dev -w apps/frontend",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w apps/frontend",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w apps/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "pptxgenjs": "^4.0.1"
  }
}
